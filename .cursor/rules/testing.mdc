---
description: "Testing Rules fuer alle AutomationOne Bereiche"
globs: "**/tests/**,**/test_*,**/*.spec.ts,**/*.test.ts"
alwaysApply: false
---

# Testing Rules

## Test-Konventionen
- Python: pytest mit fixtures aus conftest.py, async Tests mit pytest-asyncio
- Frontend: Vitest fuer Unit-Tests, Playwright fuer E2E
- ESP32: PlatformIO native test framework
- Keine Mocks fuer interne Module — nur fuer externe Services (DB, MQTT Broker, HTTP)

## Test-Benennung
- Python: `test_<was>_<szenario>` (z.B. `test_sensor_data_validates_range`)
- TypeScript: `describe('<Modul>')` + `it('should <verhalten>')`
- Dateien: `test_<modul>.py` (Python), `<Modul>.test.ts` / `<Modul>.spec.ts` (TS)

## Assertions
- Exakte Werte pruefen, keine `assert result` ohne Vergleich
- Fehlerfaelle IMMER testen (nicht nur Happy Path)
- Async-Tests muessen Timeouts haben

## MQTT-Handler Tests (Pflicht)
- Jeder Handler braucht Tests fuer: valide Message, invalide Message, fehlende Felder
- Handler-Exception-Handling verifizieren

## Backend-Tests
- `cd "/workspace/El Servador/god_kaiser_server" && poetry run pytest tests/ --timeout=120`
- 1820+ Tests; E2E braucht `--e2e` Flag + laufenden Stack
- pytest-Fixtures muessen aufgeraeumt werden (kein DB-State-Leak)

## Frontend-Tests
- `cd "/workspace/El Frontend" && npx vitest run`
- 1581+ Unit Tests (47 Test-Dateien)
- Type-Check: `npx vue-tsc --noEmit`
