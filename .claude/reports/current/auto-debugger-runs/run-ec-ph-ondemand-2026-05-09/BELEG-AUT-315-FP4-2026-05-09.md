# Beleg — AUT-315: ADC2-Pin-Risiko Verifikation

**Finding-ID:** FP4  
**Issue:** AUT-315  
**Run-ID:** run-ec-ph-ondemand-2026-05-09  
**Datum:** 2026-05-09  
**Erstellt von:** technical-manager (DB-Inspector Agent)  

---

## Fragestellung

Liegt ein pH- oder EC-Sensor auf einem ADC2-Pin des ESP32?  
Bei aktivem WiFi gibt `sensor_manager.cpp:1701-1705` stil `return 0` zurück — keine Logs, keine MQTT-Events, stille Falsch-0-Werte in der DB.

**ADC2-Pins (gesperrt bei WiFi):** GPIO 0, 2, 4, 12, 13, 14, 15, 25, 26, 27  
**ADC1-Pins (sicher):** GPIO 32, 33, 34, 35, 36, 39  

---

## DB-Query

```sql
SELECT esp_id, gpio, sensor_type, sensor_name
FROM sensor_configs
WHERE sensor_type IN ('ph', 'ec', 'soil_moisture', 'moisture')
ORDER BY esp_id, gpio;
```

---

## Ergebnis

| esp_id | GPIO | sensor_type | ADC2-Risiko? |
|--------|------|-------------|-------------|
| 18c377a3... | 32 | moisture | Nein — ADC1 |
| 18c377a3... | 33 | moisture | Nein — ADC1 |
| 63f776d4... | 32 | moisture | Nein — ADC1 |
| 63f776d4... | 33 | moisture | Nein — ADC1 |
| ae84be5f... (ESP_6B27C8) | 32 | ph | Nein — ADC1 |
| ae84be5f... (ESP_6B27C8) | 33 | ec | Nein — ADC1 |

**Prüfziel ESP_6B27C8:**
- pH-Sensor GPIO 32 → ADC1, sicher (bereits bekannt)
- EC-Sensor GPIO 33 → ADC1, sicher (neu bestätigt)

---

## Befund

**CLEAN: Kein Produktiv-Sensor auf ADC2-Pin.**

Alle 6 Einträge liegen auf GPIO 32 oder GPIO 33 (ADC1). Kein Hardware-Fix notwendig.

---

## Kein Code-Fix

Per Issue-Scope: "Falls keine Treffer: dann Issue auf 'no action needed, latentes Risiko verifiziert sauber' schließen."  
→ Keine Kabelumklemmung, kein Firmware-Fix.

---

## Strukturelle Lücke (optionales Follow-up)

`sensor_manager.cpp:1701-1705` gibt bei ADC2-Konflikt still `return 0` zurück — kein MQTT-Health-Event, kein Log-Broadcast. Das ist ein strukturelles Tracing-Gap. Für eine nachhaltige Lösung:  
**Robin entscheidet** ob das als separater Sub-Issue gegen `sensor_manager.cpp` beauftragt wird (Scope: ADC2-Guard → Sensor-Health-Event broadcasten statt stilles return 0).
