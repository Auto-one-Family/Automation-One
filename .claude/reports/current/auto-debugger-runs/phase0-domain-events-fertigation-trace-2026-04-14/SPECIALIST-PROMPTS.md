# SPECIALIST-PROMPTS — phase0-domain-events-fertigation-trace-2026-04-14

## frontend-dev

**Erledigt (PKG-01):** `useFertigationKPIs` WS-Pfad; Test `tests/unit/composables/useFertigationKPIs.ws.test.ts`.  
**Git:** nur Commits auf `auto-debugger/work`.

## Optional (PKG-02)

- `useDashboardWidgets.ts`: `FertigationPairWidget` importieren, neuen `type` (z. B. `fertigation-pair`) in `widgetComponentMap` / `WIDGET_TYPE_META` / Defaults; `WidgetConfigPanel` prüfen falls Konfigurations-UI nötig.

## Optional (PKG-03)

- `docs/FERTIGATION_WIDGET_INTEGRATION.md`: WebSocket-Beispiel auf `message.data` und Feld `value` korrigieren.
