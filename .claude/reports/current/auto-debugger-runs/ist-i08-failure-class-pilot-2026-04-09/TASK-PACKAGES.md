# TASK-PACKAGES — I08 failure_class Pilot

**Status:** erledigt (Implementierung auf Branch `auto-debugger/work`)

## PKG-01 — failure_class Pilot (Server)

- **Owner:** server-dev  
- **Akzeptanz:** drei markierte `extra={"failure_class": ...}`-Stellen; JSONFormatter-Whitelist; pytest für Formatter; LogQL in `docs/debugging/logql-queries.md`; IST-Abschnitt P1 angepasst; keine PII in Feldwerten (nur feste Kurz-Strings).
