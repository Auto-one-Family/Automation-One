# Frontend Dev Report: FIX-ED-1 — Widget Smart Defaults

## Modus: B (Implementierung)

## Auftrag
Widget Smart Defaults: Y-Range, Titel & Platzierung im Dashboard-Editor korrekt setzen.
- Fix-Block 1: WidgetConfigPanel handleSensorChange — immer Y-Range aktualisieren + Titel nur bei Auto-Titeln
- Fix-Block 2: 6 Widget-Dateien — Leer-Zustand zeigt Titel-Kontext
- Fix-Block 3: gridLayout.ts Utility + CustomDashboardView Integration
- Fix-Block 4: dashboard.store addWidget Smart Placement

## Codebase-Analyse

Analysierte Dateien:
- `WidgetConfigPanel.vue` — handleSensorChange hatte Bedingung `yMin == null && yMax == null`, updatete Range also nicht beim Sensor-Wechsel wenn bereits gesetzt. Kein Titel-Update.
- `GaugeWidget.vue`, `LineChartWidget.vue`, `SensorCardWidget.vue`, `StatisticsWidget.vue`, `HistoricalChartWidget.vue`, `MultiSensorWidget.vue` — keines hatte `title` Prop, keines zeigte Titel-Kontext im Leer-Zustand
- `CustomDashboardView.vue` addWidget() — kein `x`/`y` im `grid.addWidget()`-Aufruf, Widgets stapeln sich bei GridStack-Default-Placement
- `dashboard.store.ts` addWidget() — Widget wurde mit config.x/config.y gespeichert ohne Smart-Placement-Berechnung

## Qualitaetspruefung (8-Dimensionen)

| # | Dimension | Status |
|---|-----------|--------|
| 1 | Struktur & Einbindung | gridLayout.ts korrekt in src/utils/ platziert, @/ Import in beiden Konsumenten |
| 2 | Namenskonvention | findFirstFreePosition camelCase, Datei kebab-case |
| 3 | Rueckwaertskompatibilitaet | title Props optional — kein Breaking Change. addWidget im Store ueberschreibt pos.x/y — bestehende gespeicherte Layouts nicht betroffen |
| 4 | Wiederverwendbarkeit | findFirstFreePosition als reines Utility — wiederverwendbar fuer AddWidgetDialog |
| 5 | Speicher & Ressourcen | Keine Subscriptions, kein State — kein Cleanup noetig |
| 6 | Fehlertoleranz | ?? 0 Fallbacks fuer fehlende x/y/w/h. isAutoGeneratedTitle prueft null/undefined |
| 7 | Seiteneffekte | handleSensorChange emitiert update:config — Verhalten unveraendert, nur erweiterter Update-Scope |
| 8 | Industrielles Niveau | Kein any in gridLayout.ts, TypeScript strict kompatibel |

## Cross-Layer Impact

Keine Server-Aenderungen. Kein WebSocket-Kontrakt betroffen. Reine Frontend-Logik.

## Ergebnis

### Fix-Block 1: WidgetConfigPanel.vue
- `isAutoGeneratedTitle()` Hilfsfunktion hinzugefuegt
- `handleSensorChange()` ersetzt: yMin/yMax IMMER aktualisiert, Titel nur bei Auto-Titeln gesetzt

### Fix-Block 2: Widget Leer-Zustaende (5 Dateien geaendert, 1 unveraendert)
- GaugeWidget.vue — title Prop hinzugefuegt + Leer-Text aktualisiert
- LineChartWidget.vue — title Prop hinzugefuegt + Leer-Text aktualisiert
- HistoricalChartWidget.vue — title Prop hinzugefuegt + Leer-Text aktualisiert
- MultiSensorWidget.vue — title Prop hinzugefuegt + Leer-Text aktualisiert
- SensorCardWidget.vue — title Prop hinzugefuegt + option-Text aktualisiert
- StatisticsWidget.vue — title Prop war bereits vorhanden (in Props via withDefaults), Leer-Text aktualisiert

### Fix-Block 3a: gridLayout.ts (neue Datei)
- Pfad: El Frontend/src/utils/gridLayout.ts
- findFirstFreePosition(widgets, newW, newH, columns=12) implementiert

### Fix-Block 3b: CustomDashboardView.vue
- Import findFirstFreePosition aus @/utils/gridLayout
- addWidget() Funktion: pos berechnet, x: pos.x, y: pos.y in grid.addWidget() uebergeben

### Fix-Block 4: dashboard.store.ts
- Import findFirstFreePosition aus @/utils/gridLayout
- addWidget() Funktion: pos aus findFirstFreePosition, Widget erhaelt x: pos.x, y: pos.y

## Verifikation

npm run build: Exit-Code 0, keine TypeScript-Fehler
vue-tsc -b && vite build: 3068 modules transformed, built in 9.49s

## Empfehlung

Keine weiteren Agenten noetig. Alle Aenderungen sind rein Frontend-seitig.
