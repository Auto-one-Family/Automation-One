# El Frontend — lokale Entwicklung (Vite lädt .env.development)
# Kopie nach .env.development nur nötig, wenn du von diesem Template startest.
# Standard: Werte stehen bereits in .env.development im Repo.

VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000
VITE_LOG_LEVEL=debug
